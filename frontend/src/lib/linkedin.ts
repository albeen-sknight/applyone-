export const linkedInCallbackUrl = "https://applyone.pages.dev/auth/callback";

export function getLinkedInStatus() {
  return {
    configured: false,
    message: "LinkedIn OAuth se configurara en una fase posterior."
  };
}
